;; Kanata Control Commands
;; Leader -> k -> key

;; Sequences
(defseq reload-config (k r))

;; Virtual keys
(defvirtualkeys
  reload-config (multi lrld (cmd terminal-notifier -title "Kanata" -message "Config reloaded"))
)
